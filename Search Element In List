#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <stdbool.h>


int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */  
    int n;
    scanf("%d",&n);
    int arr[n],i;
    for(i=0;i<n;i++){
        scanf("%d",&arr[i]);
        // printf("%d ",arr[i]);
    }
    int k;
    scanf("%d",&k);
    bool flag = false;
    for(i=0;i<n;i++){
        if(arr[i]==k){
            printf("Found");
            flag = true;
        }
    }
    if(!flag){
        printf("Not Found");
    }
    
    
    return 0;
}
